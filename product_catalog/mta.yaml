_schema-version: 3.3.0
ID: product_catalog
description: A simple CAP project.
version: 1.0.0
modules:
  - name: product_catalog-srv
    type: nodejs
    path: gen/srv
    requires:
      - name: product_catalog-db
      - name: product_catalog-auth
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    parameters:
      buildpack: nodejs_buildpack
      instances: 1
    build-parameters:
      builder: npm-ci
  - name: product_catalog-db-deployer
    type: hdb
    path: gen/db
    requires:
      - name: product_catalog-db
    parameters:
      buildpack: nodejs_buildpack
  - name: product_catalog-app-content
    type: com.sap.application.content
    path: .
    requires:
      - name: product_catalog-repo-host
        parameters:
          content-target: true
    build-parameters:
      build-result: resources
      requires:
        - artifacts:
            - dbproductos.zip
          name: dbproductos
          target-path: resources/
  - name: dbproductos
    type: html5
    path: app/productos
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm install
        - npm run build:cf
      supported-platforms:
        []
resources:
  - name: product_catalog-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: product_catalog-auth
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        oauth2-configuration:
          credential-types:
            - binding-secret
            - x509
        tenant-mode: dedicated
        xsappname: product_catalog-${org}-${space}
      path: ./xs-security.json
      service: xsuaa
      service-plan: application
  - name: product_catalog-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-name: product_catalog-html5-service
      service-plan: app-host
  - name: product_catalog-destination-service
    type: org.cloudfoundry.managed-service
    parameters:
      config:
        HTML5Runtime_enabled: false
        init_data:
          instance:
            destinations:
              - Authentication: NoAuthentication
                Name: ui5
                ProxyType: Internet
                Type: HTTP
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
      service: destination
      service-name: product_catalog-destination-service
      service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
